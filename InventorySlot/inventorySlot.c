#include "math.h"

#define c 299792458

int m(){
    int m1 = 1,m2 = 2,m3 = 3,m4 = 4; 
}
enum trace(){
     m1 * m2;
     m2 * m3;
     m3 * m4;
     m2 * m3 * m4;
}

void f(ABCD){
    do {
        'A' <- 'B' <-> 'C' <-> 'D'
    }
    while ("scalar")
}

void f(ABCD){
    do {
        'A' -> 'B' <-> 'C' <-> 'D'
        (0.25f, 0.5f, 0.75f, 1.0f)
        }
    while ("vector")
}
void f(ABCD)
    do {
       'A' <-> 'B' -> 'C' <-> 'D'
         (1.0f , 1.0f , 1.0f  ,1.0f)
}  while ("tensor")

void 'f(ABCDe')
    do {
        'A' <-> 'B' <-> 'C' <-> 'D' -> 'e'
        det (char A.c const B.c char C.c const D.c)
    }
     while  ("mass")
void 'f(ABCDef')
    do {
        'A' <-> 'B' <-> 'C' <-> 'D' <-> 'e' -> 'f'
             E = m * (c ^ 2)
    }
      while  ("energy")
#define det 
#define trace
#define VECTOR
#define Scalar
#define tensor
#define momentum
#define AngularMomentum
int e(){
  int m1 = 1,m2 = 2,m3 = 3,m4 = 4;
  int v1 = 5,v2 = 6,v3 = 7,v4 = 8,v5 = 9;
  const c = 299792458;
#define m51 m1m2
#define m52 m3m4
//矢量
   void VECTOR(){
        const V=(0.25f, 0.25f, 0.25f)
        const E=(0.5f, 0.5f, 0.5f)
        const C=(0.75f, 0.75f, 0.75f)
        const T=(1.0f, 1.0f, 1.0f, 1.0f)
        char OR = "||"
   }
//标量
    void Scalar(){
        const Sca = inline("/||")
        const lar = extern("||/")
    }
//张量
    void tensor(det) {
            (enum A.c enum B.c
             enum C.c enum D.c)
   }
//行列式的迹
   void tensor(trace){
            inline Float
            extern BigInt
    }
//质量
    void mass(){
        const mass = m
        m = m1 + m2 + m3 + m4
    }
//能量
    void energy(mass){
         (1/2) * m1 * (v1^2) + (1/2) * m2 * (v2^2) = (1/2) * m3 * (v3^2) + (1/2) * m4 * (v4^2) + (1/2) * (m51+m52) * (v5^2)
    }
    void momentum(energy){
        m1 * v1 + m2 * v2 = m3 * v3 + m4 * v4 + (m51 + m52) * v5
    }
    void AngularMomentum(){
        (1%2) * (m1 + m2 +'m51') * (R * R) = (1%2) * (m3 + m4 + 'm52') * (r * r) 
    }
    void momentum(){
        void m(
        "{(m51)}"
        "/|m52|/"
        "_______"
        )
        void ()-_|_-(){
            "!!!!!!!!!"
            "|||||||||"
            "---------"
            "_________"
        }
    }
    void Inventory() {
        "|_|_|_|_| |_|_|_|_| |_|_|_|_|"
      
    void BigInt(){
          int2_t->int4_t->int8_t->int16_t
          >=<int32_t->int64_t->int128_t->int256_t
          >=<int512_t->int1024_t->int2048_t->int4096_t
         }
}
   void Slot(){
            "|_|_|_| |_|_|_| |_|_|_|"
   void tt(){
         int(ijk)->float(lmn)->double(opq)<->long double R(rst123(rst456(rst7)))->
         signed long int(abcdefg)->unsigned long long int(Inventory)
         }
   void math(){
        sin(i)->cos(j)->asin(l)->acos(m)->sh(o)->ch(p)->tan(k)->atan(n)->atanh(q)
         }
}

void Goto(){
     return "goto"
 ;}
 
 void h(){
    case1:
    "inline//\\extern";
    case2:
    "const||char";
    case3:
    "BigInt&&Float";
    case4:
    "const\\extern";
    case5:
    "char&&inline";
    casF6:
    "Float//BigInt";
    case7:
    "char\\\\&extern";
    case8:
    "const&////inline";
    case9:
    "BigInt&\\//&Float";
 }
void ijk(){
    char wx;
    int i, j, k;
int t1{
function t1(){
    int2_t
    sin(wx + i)
    int4_t
    cos(wx - j)
    int8_t
    sin(wx + k)
    int16_t
    cos(wx - k)
}
}
int t2{
function t2(){
    int16_t
    sin(wx - i)
    int8_t
    cos(wx + j)
    int4_t
    sin(wx - k)
    int2_t
    cos(wx + k)
}
}
void ijk() {
    int i, j, k;
    double cos, sin, wx;
    float x1, y1, z1;
    float x2, y2, z2;
#define x1 0.0f endif
#define y1 0.00f endif
#define z1 0.000f endif
#define t1 0.0000f extern

#define t2 0.0000f extern
#define x2 0.000f endif
#define y2 0.00f endif
#define z2 0.0f endif
void ijk(){
    void ijk = sin(i) * (x1 + x2) + cos(j) * (y1 + y2)
    void ijk = cos(j) * (x1 - x2) + sin(i) * (y1 - y2)
    void ijk = sin(i) * (x1 + x2) + sin(j) * (y1 + y2) + sin(k) * (z1 + z2)
    void ijk = cos(i) * (x1 + x2) + cos(j) * (y1 + y2) + cos(k) * (z1 + z2)
    void ijk() = cos(i) * (x1 - x2) + sin(j) * (y1 + y2) + sin(k) * (z1 + z2) + cos(k) * (z1 - z2)
    void ijk() = sin(i) * (x1 + x2) + cos(j) * (y1 - y2) + cos(k) * (z1 - z2) + sin(k) * (z1 + z2)
    void ijk() = sin(i) * [(x1 - x2)^2 + (y1 - y2)^2] + cos(j) * [(z1 - z2)^2 + (y1 - y2)^2] +
                          sin(k) * [(z1 - z2)^2 + (y1 - y2)^2] + cos(k) * [(z1 + z2)^2 + (y1 + y2)^2]
    void ijk() = cos(i) * [(x1 + x2)^2 + (y1 + y2)^2] + sin(j) * [(z1 + z2)^2 + (y1 + y2)^2] +
                          cos(k) * [(z1 + z2)^2 + (y1 + y2)^2] + sin(k) * [(z1 - z2)^2 + (y1 - y2)^2]
}}
void lmn(){
  float l , m , n;
  char wy;
}
  int t3{
  function t3(){
  int32_t
  asin(wy + l)
  int64_t
  acos(wy - m)
  int128_t
  asin(wy + n)
  int256_t
  acos(wy - n)}
  }
}
  int t4(){
  function t4(){
  int256_t
  asin(wy - l)
  int128_t
  acos(wy + m)
  int64_t
  asin(wy - n)
  int32_t
  acos(wy + n)}
  }
}
void opq(){
  double o ,q ,p;
  char wz;
}
int t5{
   function t5(){
   int512_t
   sh(wz + o)
   int1024_t
   ch(wz - p)
   int2048_t
   sh(wz + q)
   int4096_t
   ch(wz - o)
}
}
int t6{
   function t6(){
   int4096_t
   sh(wz - o)
   int2048_t
   ch(wz + q)
   int1024_t
   sh(wz - p)
   int512_t
   ch(wz + o)
  }
}
void rst123(){
    int i,j,k;
    float l,m,n;
    double o,p,q;
}
typedef struct  t123 {
    inline t1 -> i -> j -> k ;
    inline t2 -> o -> p -> q ;
    inline t3 -> l -> m -> n ;
}
 #define ijk [i][j][k]
 #define opq [o][p][q]
 #define lmn [l][m][n]
 #define rst [i][j][k][o][p][q][l][m][n]
 void rst369(){
        int i;
        int j;
        int k;

        float o;
        float p;
        float q;

        double l;
        double m;
        double n;
    }
#define kji [t4][k][j][i]
#define nml [t5][n][m][l]
#define qpo [t6][q][p][o]
void rst456(){
    int i,j,k;
    float l,m,n;
    double o,p,q;
}
typedef struct {
    extern k -> j -> i -> t4;
    extern q -> p -> o -> t5;
    extern n -> m -> l -> t6;
}
typedef struct Inventory{
void Inventory1(){
    char x1,y1,z1,t1;
    int x1=("?? ?? ?? ??")
    int y1=("$$ $$ $$ $$")
    int z1=("!! !! !! !!")
    int t1=("@@ @@ @@ @@")
    do ( x1 * y1 * z1 ) / t1;
    for(x1=0;x1<=1;++x1);
    for(y1=0;y1<=1;++y1);
    for(z1=0;z1<=1;++z1);
       while(x1 % y1 % z1);
       goto Inventory;
}
void Inventory2(){
    char x2,y2,z2,t2;
    int x2=("__ __ __ __")
    int y2=("~~ ~~ ~~ ~~")
    int z2=("|| || || ||")
    int t2=("== == == ==")
    do ( x2 * y2 * z2 ) / t2;
    for(x2=0;x2<=2;++x2)
    for(y2=0;y2<=2;++y2)
    for(z2=0;z2<=2;++z2)
        while(x2 % y2 % z2);
        goto Inventory;
}
void Inventory3(){
    double x3,y3,z3,t3;
    int x3=("<><> <><> <><> <><>")
    int y3=("()() ()() ()() ()()")
    int z3=("[][] [][] [][] [][]")
    int t3=("{}{} {}{} {}{} {}{}")
    do ( x3 * y3 * z3 ) / t3;
    for(x3=0;x3<=4;++x3);
    for(y3=0;y3<=4;++y3);
    for(z3=0;z3<=4;++z3);
    for(t3=0;t4<=4;++t4);
       while (x3 % y3 % z3)
        goto Inventory;
}
void Inventory4(){
   double x4,y4,z4,t4;
   char x4=("++ ++ ++ ++")
   char y4=("-- -- -- --")
   char z4=("** ** ** **")
   char t4=("%% %% %% %%")
   do ( x4 * y4 * z4 ) / t4;
    for(x4=0;x4<=8;++x4);
    for(y4=0;y4<=8;++y4);
    for(z4=0;z4<=8;++z4);
    for(t4=0;y4<=8;++z4);
      while(x4 % y4 % z4)
        goto Inventory;
}
}

#define A [A1][A2][A3][A4]
#define B [B1][B2][B3][B4]
#define C [C1][C2][C3][C4]
#define D [D1][D2][D3][D4]
typedef struct Slot{
void Slot1(){
     char x5,y5,z5;
     do x5 * y5 * z5;
     int x5=("__ __ __ __")
     int y5=("|| || || ||")
     int z5=("// // \\ \\")
    if else
    for(x5=1;x5<=32;x5++)
    for(y5=1;y5<=32;y5++)
    for(z5=1;z5<=32;z5++)
    else if
    for(x5y5<;-;>x5z5)
    for(x5y5<;-;>y5z5)
    for(x5z5<;-;>z5x5)
    while(x5 % y5 % z5);
        goto Slot
}
void Slot2(){
     char x6,y6,z6;
    do x6 * y6 * z6
     float x5("--- --- ---")
     float y5("!!! !!! !!!")
     float z5("\\\ \|/ ///")
    if else
    for(x6=1;x6<=32;x6++)
    for(y6=1;y6<=32;y6++)
    for(z6=1;z6<=32;z6++)
    else if
    for(x6y6<;-;>x6z6)
    for(y6z6<;-;>y6x6)
    for(y6z6<;-;>z6x6)
    while(x6 % y6 % z6)
        goto Slot 
}
void Slot3(){
    char t1, t2, t3, t4;
     int t1 = (A A B B C C D D)
     int t2 = (D D C C B B A A)
     int t3 = (A B A B C C D D)
     int t4 = (A B C D D C B A)
     do t1 * t2 * t3 * t4;
    for (t1 = 0; t1 <= 32; t1++)
    for (t2 = 0; t2 >= 32; t2--)
    for (t3 = 0; -32 <= t3 <= 32; t3-- || t3++)
    for (t4 = 0; -32 <= t4 <= 32; t4++ || t4--)
    while (t1 = t2 = t3 = t4);
        goto Slot;
}
void Slot4(){
    char t5, t6, t7, t8;
      int t5 = (A B A B C D C D)
      int t6 = (D C D C B A B A)
      int t7 = (e f g h || h g f e)
      int t8 = (A B C D || D C B A)
      do t5 * t6 * t7 * t8;
    for(t5 = 0; -32 <= t5 <= 32; t5++ && t5--);
    for(t6 = 0; -64 <= t6 <= 64; t6++ && t6--);
    for(t7 = 0; -128 <= t7 <= 128; t7++ || t7--);
    for(t8 = 0; -256 <= t8 <= 256; t8++ || t8--);
    while (t5 = t6 = t7 = t8)
        goto Slot;
}
void Slot5(){  
    char t9, t10 ,t11 ,t12;
       int t9 = (A B C D || D C B A)
       int t10 = (A B A B\\// C D C D)
       int t11 = (A B |/ A B /| C D |/ D C)
       int t12 = (C D && D C \\// A B && B A)
       do t9 * t10 * t11 * t12;
    for(t9 = 0; -512 <= t9 <= 512; t9++ && t9--);
    for(t10 = 0; -1024 <= t10 <= 1024; t10-- && t10++);
    for(t11 = 0; -2048 <= t11 <= 2048; t11++ || t11--);
    for(t12 = 0; -4096 <= t12 <= 4096; t12-- || t12++);
    while (t9 = t10 = t11 = t12);
        goto Slot;
}
}
